name: Build PrintCheck 3.4.0-alpha12 APK
on: [workflow_dispatch, push]

jobs:
  apk:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      ANDROID_HOME: /usr/local/lib/android/sdk
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Install Android SDK 35
        shell: bash
        run: |
          export PATH="$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools:$PATH"
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platforms;android-35" "build-tools;35.0.0"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'
      - name: Prepare canonical alpha12 source
        run: python3 printcheck-build/prepare_alpha12.py .
      - name: Verify alpha12
        shell: bash
        run: |
          grep -q "versionName '3.4.0-alpha12'" .printcheck-alpha12/app/build.gradle
          grep -q "versionCode 340012" .printcheck-alpha12/app/build.gradle
          grep -q "compact_article_results_v1" .printcheck-alpha12/app/src/main/java/ru/printcheck/android/MainActivity.java
          grep -q "batch_results_screen_v1" .printcheck-alpha12/app/src/main/java/ru/printcheck/android/MainActivity.java
          grep -q "Подробнее  ▾" .printcheck-alpha12/app/src/main/java/ru/printcheck/android/ResultView.java
          grep -q "renderBatch(" .printcheck-alpha12/app/src/main/java/ru/printcheck/android/ResultView.java
      - name: Core tests
        run: bash .printcheck-alpha12/tests/run_core.sh
      - name: Build APK
        run: gradle --no-daemon --stacktrace assembleDebug
      - name: Package source
        shell: bash
        run: |
          cd .printcheck-alpha12
          zip -qr "$GITHUB_WORKSPACE/PrintCheck_Android_3.4.0-alpha12_source.zip" . -x '*/build/*' '*.keystore'
      - name: Collect APK
        shell: bash
        run: |
          cp .printcheck-alpha12/app/build/outputs/apk/debug/app-debug.apk PrintCheck_Android_3.4.0-alpha12.apk
          sha256sum PrintCheck_Android_3.4.0-alpha12.apk
      - uses: actions/upload-artifact@v4
        with:
          name: PrintCheck_Android_3.4.0-alpha12_BUILD
          path: |
            PrintCheck_Android_3.4.0-alpha12.apk
            PrintCheck_Android_3.4.0-alpha12_source.zip
          if-no-files-found: error
          retention-days: 7
